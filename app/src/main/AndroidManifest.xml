<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.davidalexfarina.pedidosmobile">
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".modulo_usuario.EditarUsuarioActivity"></activity>
        <activity android:name=".modulo_usuario.UsuariosActivity" />
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.MesasActivity" />
        <activity android:name=".activity.PedidoDaMesaActivity" />
        <activity android:name=".BebidasAdapter" />
        <activity android:name=".PorcaoAdapters" />
        <activity android:name=".adapter.ListaPedidoConfirmadoAdapters" />
        <activity android:name=".modulo_pedido_produto.activity.SolicitacaoDaMesaActivity" />
        <activity android:name=".modulo_pedido_produto.activity.EditarProdutoActivity" />
    </application>

</manifest>